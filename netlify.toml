[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirect SPA — toutes les routes → index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
